1. nn.Sequential 안에 넣고 싶을 때  
2. 모델 구조를 출력했을 때 활성화가 레이어로 보이게 하고 싶을 때  
3. 다른 레이어처럼 일관된 형태로 쌓고 관리하고 싶을 때

그래서
- 그냥 계산만 하면: torch.tanh(x), torch.sigmoid(x)
- 모델 블록으로 넣으면: nn.Tanh(), nn.Sigmoid()

즉 연산은 같고, 코드를 어떻게 구성하고 관리하느냐의 차이입니다.